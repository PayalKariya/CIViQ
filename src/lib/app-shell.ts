/** Page background for marketing + dashboard shells (light theme). */
export const appPageBackgroundClass =
  'min-h-screen bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50';

/** Sticky top bar shared by marketing chrome and app dashboards. */
export const appHeaderSurfaceClass =
  'sticky top-0 z-50 border-b border-gray-200/80 bg-white/85 text-slate-900 shadow-sm backdrop-blur-md';
